[project]
name = "homework"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "Hugo Ortega", email = "hugo23729@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.5.3",
]

[project.scripts]
homework = "homework:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
